#프로젝트 속성 -> C/C++ -> 전처리기 -> 전처리기 정의:  에서 다음 세 값을 추가
	WIN32; WPCAP; HAVE_REMOTE;
# C/C++ 메뉴가 보이지 않는다면 프로젝트에 c++ 파일을 하나 추가한 후 다시 확인해 보기

#프로젝트 속성 -> 링커 -> 일반 -> 추가 라이브러리 디렉터리:  에서 .lib 파일이 있는 경로를 추가
#프로젝트 속성 -> 링커 -> 입력 -> 추가 종속성: 에서 아래 두 .lib 파일을 추가
	ws2_32.lib; wpcap.lib;
#프로젝트 속성 -> C/C++ -> 일반 -> 추가 포함 디렉터리: 에서 .h 파일이 있는 경로를 추가
#pcap 기능을 사용할 c++ 코드에서 pcap header 파일을 포함하도록 아래와 같이 include 라인을 추가
#include <pcap.h>
#main 함수를 만든 후, 에러 없이 컴파일 된다면 pcap을 사용할 준비가 완료된 것임